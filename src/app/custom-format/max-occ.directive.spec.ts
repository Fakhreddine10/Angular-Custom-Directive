import { MaxOccDirective } from './max-occ.directive';

describe('MaxOccDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxOccDirective();
    expect(directive).toBeTruthy();
  });
});
